<div class="postitem" id=post.id>
  <div class="item">
    <h3><a class="link" href="<%= @post.url %>"> <%= @post.title %></a></h3>
    <h4>dummy_author, Created at: <%= @post.created_at.strftime("%H:%M") %></h4>
    <p><%= @post.body %></p>
  </div>
</div>
